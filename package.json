{
  "name": "after-me-starter-app",
  "version": "0.1.0",
  "description": "Minimal Node.js starter scaffold for After-me-starter-app",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --colors --verbose",
    "lint": "echo \"no lint configured\""
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "jest": "^29.6.0"
  }
}
